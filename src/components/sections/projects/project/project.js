import React from "react"

import "./project.scss"

const project = (props) => {
    return (
        <div className="project">
            <h3>{ props.title }</h3>
        </div>
    )
}

export default project