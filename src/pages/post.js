import React from "react"
import Layout from "../components/layout"

const post = (props) => {
    return (
        <Layout>
            <div>
                This is a Post
            </div>
        </Layout>
    )
}

export default post